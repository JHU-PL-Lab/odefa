r = fun i -> ( ir = i; );

rr = r ~ fun
                    ? fun smatch -> (
                      smatchreturn = 0;
                    )
                    : fun santimatch -> (
                      x = 0;
                      santimatchreturn = x x;
                    );
