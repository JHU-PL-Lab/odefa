sum = fun self ->
(
  fun_ =  fun num ->
    (one = 1;
    is_one = num == one;
    ret = is_one~true?
      fun base -> (val = num) :
      fun rec ->
        (rec_one = 1;
        next = num - rec_one;
        self2 = self self;
        other = self2 next;
        res = num + other
      );
  ));
  three = 3;
  sum_ = sum sum;
  result = sum_ three;
