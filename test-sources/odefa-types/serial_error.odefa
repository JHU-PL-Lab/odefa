# EXPECT-WELL-FORMED
# EXPECT-TYPE-ERROR ~result [0] [ "sum1 = a + b" "b = false" "int" "bool" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

a = input;
b = false; # Only first error should be found
sum1 = a + b;
c = input;
d = false;
sum2 = c + d;
