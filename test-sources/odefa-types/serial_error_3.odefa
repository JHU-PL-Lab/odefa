# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR ~result [] [ "r = x and tru" "zero = 0" "bool" "int" "1" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

zero = 0;
one = 1;
tru = true;

f = fun x -> ( r = x and tru );
fans = f zero;

z = fans + one;
