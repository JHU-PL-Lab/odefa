# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-ANALYSIS-STACK-IS 4ddpa
# EXPECT-TYPE-ERROR ~result [] [ "b = a <= tru" "tru = true" "int" "bool" "1" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

tru = true;
one = 1;
three = 3;

f = fun s -> (
    f0 = fun a -> (
        b = a <= tru; # Type error
        r = b ? (
                r1 = a
            ) : (
                ss = s s;
                v = a - one;
                r2 = ss v;
            );
        n1 = r;
    );
);
ff = f f;
z = ff three;
