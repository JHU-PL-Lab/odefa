# EXPECT-WELL-FORMED
# EXPECT-STUCK
# EXPECT-TYPE-ERROR ~result [] [ "ry = y or fls"  "one = 1"  "bool" "int" ] [ "rx = x and tru" "zero = 0" "bool" "int" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

zero = 0;
one = 1;
tru = true;
fls = false;

f = fun x -> ( rx = x and tru );
g = fun y -> ( ry = y or fls );

a = f zero;
b = g one;
c = a and b;
