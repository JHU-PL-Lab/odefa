# EXPECT-WELL-FORMED
# EXPECT-STUCK
# TODO: Add test for type error

x1 = 0;
x2 = false;

m1 = x1 ~ int;
m2 = x2 ~ int;
m = m1 and m2;

mres = m ? ( sum = x1 + x2 ) : ( ab = abort[m1, m2] );
t = mres;
