# EXPECT-WELL-FORMED
# EXPECT-TYPE-ERROR ~result [0, 0] [ "sum1 = a + b" "b = true" "int" "bool" ] [ "sum3 = sum1 + b" "b = true" "int" "bool" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

a = input;
b = true;
sum1 = a + b;
c = input;
sum2 = sum1 + c;
sum3 = sum1 + b;
