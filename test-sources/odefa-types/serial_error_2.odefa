# EXPECT-WELL-FORMED
# EXPECT-TYPE-ERROR ~result [0] [ "sum1 = a + b" "b = true" "int" "bool" ]
# EXPECT-ALL-TYPE-ERRORS-FOUND

a = input;
b = true; # Only first error should be found
sum1 = a + b;
c = input;
sum2 = sum1 + c;
