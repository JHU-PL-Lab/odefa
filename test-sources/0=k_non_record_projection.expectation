QUERY: e, r;
ANALYSES: 0DDPA, 0PLUME, SPLUME, OSKPLUME, OSMPLUME;
RESULTS: 0DDPA(e)= `{{}}`,
         0DDPA(r)= `{int}`,
         0PLUME(e)= `{{}}`,
         0PLUME(r)= `{int}`,
         SPLUME(e)= `{{}}`,
         SPLUME(r)= `{int}`,
         OSKPLUME(e)= `{{}}`,
         OSKPLUME(r)= `{int}`,
         OSMPLUME(e)= `{{}}`,
         OSMPLUME(r)= `{int}`
;
CONSISTENCIES: 0DDPA = INCONSISTENCIES_AT z &
               0PLUME = INCONSISTENCIES_AT z &
               SPLUME = INCONSISTENCIES_AT z &
               OSKPLUME = INCONSISTENCIES_AT z &
               OSMPLUME = INCONSISTENCIES_AT z
;
WELL_FORMED;
