# EXPECT-WELL-FORMED
# EXPECT-EVALUATE
# EXPECT-ANALYSIS-LOOKUP-FROM-END ~result {int}

let match_fn v =
    match v with
    | `steve (int) -> 0
    | `alex (bool) -> 1
    end
in
let s = `steve (2 + 2) in
let a = `alex (true and false) in
(match_fn s) + (match_fn a)
