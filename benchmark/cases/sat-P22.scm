(define phi
  (lambda (x1)
  (lambda (x2)
  (lambda (x3)
  (lambda (x4)
  (lambda (x5)
  (lambda (x6)
  (lambda (x7)
  (lambda (x8)
  (lambda (x9)
  (lambda (x10)
  (lambda (x11)
  (lambda (x12)
  (lambda (x13)
  (lambda (x14)
  (lambda (x15)
  (lambda (x16)
  (lambda (x17)
  (lambda (x18)
  (lambda (x19)
  (lambda (x20)
  (lambda (x21)
  (lambda (x22)
    (and (or x1 x2) (or x1 (not x2) (not x3)) (or x3 x4) (or (not x4) x1) (or (not x2) (not x3)) (or x4 x2)))))))))))))
(define try (lambda (f) (or (f #t) (f #f))))
(define sat-solve-22 (lambda (p)
  (try (lambda (n1)
  (try (lambda (n2)
  (try (lambda (n3)
  (try (lambda (n4)
  (try (lambda (n5)
  (try (lambda (n6)
  (try (lambda (n7)
  (try (lambda (n8)
  (try (lambda (n9)
  (try (lambda (n10)
  (try (lambda (n11)
  (try (lambda (n12)
  (try (lambda (n13)
  (try (lambda (n14)
  (try (lambda (n15)
  (try (lambda (n16)
  (try (lambda (n17)
  (try (lambda (n18)
  (try (lambda (n19)
  (try (lambda (n20)
  (try (lambda (n21)
  (try (lambda (n22)
    ((((((((((((((((((((((p n1) n2) n3) n4) n5) n6) n7) n8) n9) n10) n11) n12) n13) n14) n15) n16) n17) n18) n19) n20) n21) n22)
  ))))))))))))))))))))))))))))))))))))))))))))
))
(sat-solve-22 phi)
